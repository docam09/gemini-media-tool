# Dependencies for users who prefer pip over Poetry (e.g. Windows users).
# Keep in sync with pyproject.toml.
fastapi[standard]>=0.115,<0.120
google-genai>=0.8,<2.0
pydantic>=2.9,<3.0
python-dotenv>=1.0,<2.0
