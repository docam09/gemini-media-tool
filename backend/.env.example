# Copy to backend/.env and fill in your real key.
# Get one for free at https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Optional override (defaults to gemini-2.5-flash)
# GEMINI_MODEL=gemini-2.5-flash
# Deployment only: comma-separated list of frontend origins allowed by CORS.
# localhost is always allowed; add your production domain(s) here.
# ALLOWED_ORIGINS=https://translator.example.com
