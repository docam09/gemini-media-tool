# Optional: server-side fallback for Gemini API key.
# Prefer letting users bring their own key via the frontend (header X-Gemini-Api-Key).
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
DATABASE_PATH=./app.db
